# Native Host and Viewer workflow inventory

## Reference and status rules

- **Reference:** `webmaster19083-tech/Curator-rs` at `3fc9aaca46ca0246e7a30f393f2dd799e82656f0` (pinned 2026-09-22). Browser behaviour is from `static/`; the API contract is `src/routes/mod.rs`.
- **Status:** `implemented` requires an appropriate native screen and typed service. `verified` also requires service/HTTP tests where applicable and explicit Windows/Linux manual evidence. `backend only`, `partial`, and `absent` are not complete.
- **Roles:** Host (H) has local library and Local Admin rights; Viewer (V) has only negotiated remote operations; Server (S) keeps the browser client. V must be denied Host-only work at UI and service boundaries.
- **Baseline:** routes are broad, but the Host is a list prototype, Viewer is a connection form, and `native::LocalClient` still adapts route handlers. No workflow is verified merely because an HTTP handler exists.

## Architecture gates

- [ ] **A-01 Typed service boundary** — Extract `library`, `settings`, `discovery`, `storage`, `downloads`, `jobs`, `backup`, `export`, `media`, and `playback` services; adapters retain every path, method and format. H/S full; V scoped. Status: partial. Library list queries, pages, validation, and admission use a typed transport-independent service directly from Host and through the unchanged Server `GET /api/media` adapter. Settings reads use a shared audience-aware service from Host and the `GET /api/settings` adapter, with direct/HTTP parity and remote path-denial tests. Storage accounting has typed categories, disk and source usage models shared by Host and the `GET /api/storage` adapter, with direct/HTTP parity and Viewer, shutdown, and maintenance tests. Discovery provider descriptors and catalog live in a typed service used directly by Host and through `GET /api/search/providers`. Backup inventory and validated download paths use a typed service shared by Host and Server admin adapters, with Viewer/shutdown denial and direct/HTTP parity tests. Maintenance job list, lookup and start use a typed service from Host and the existing Server admin adapters, with direct/HTTP parity and rejection tests. Source-list export and import now use shared services from Host and the existing Server adapters, retaining their payloads, duplicate handling and reminder update with direct/HTTP parity and Viewer/shutdown/maintenance admission tests. Native Host source creation also uses the shared service behind Server `POST /api/sources`. Session admission, media byte-range planning, typed download activity, and global/per-source download controls also have direct/HTTP tests. Media rating, approval, guarded undo and bulk tag add now have direct Host service calls and Server adapters with direct/HTTP tests and boundary denials. Settings writes, discovery search, metadata/package import/export, remaining media mutations, download control compatibility JSON, the broader extraction, and Server-side Viewer role authority remain open.
- [ ] **A-02 Operation control** — Separate interaction, jobs, thumbnails and playback; use bounded queues, cancellation, deduplication, progress and actionable errors. Status: partial. Native Host/Viewer use bounded regular (128), control (16), image (1), and preference-save (1) work queues; preference snapshots coalesce to the newest value. A full regular queue reports rejected actions and stale page/preview results cannot replace the current view. Closing the window signals every worker, cancels pending asynchronous image/control/general work, and stops queued work from draining; the general worker checks for shutdown every 100 ms when idle. Unit tests cover control admission, preference coalescing, and in-flight cancellation. Synchronous folder selection and local blocking work can still delay close; playback seek, measured navigation/quit latency, Windows manual, and Linux manual results remain open.
- [ ] **A-03 Typed durable models** — Versioned settings, discovery capability, storage, activity, job, backup, export, media and playback models. Preserve unknown Viewer preferences. Status: partial (`db::Settings` coexists with ad-hoc JSON).
- [ ] **A-04 Lifecycle authority** — Service boundary owns permissions, maintenance lease, library lock and shutdown; listener failure remains visible while offline H works. Status: partial. Session start/control, library list and storage accounting share direct-service admission for maintenance and shutdown; direct and HTTP parity and denials have automated tests. Library list and storage accounting deny a Viewer local library. The system handshake now advertises a read-only Viewer permission set, and the native remote client enforces it before network calls for reads, edits, discovery, playback and session control. Server-side authenticated role authority, other operations, occupied port, disconnect, and Windows/Linux manual results remain open.

## Shell and appearance

- [ ] **U-01 Native shell** — File/Tools menus; Library, Player, Manage; source/group tree/counts/Add source; location title, compact navigation and contextual actions. H/V as permitted. Status: partial. The shell has Library, Player, Manage, a current-location title, and nested source/group navigation; media counts come from the shared library summary, with native/HTTP parity and nested ordering tested. Windows Host manual confirmed Library and Manage navigation at 1200×800 using an empty disposable library, then nested group/source counts and changing location titles with 120 disposable items. Menus, contextual actions, compact navigation, Viewer and Linux manual results remain open.
- [ ] **U-02 Manage destinations** — direct Discover, Organization, Activity and Settings navigation with retained state. H/V as permitted. Status: partial. The Slint Manage shell now has direct destination tabs, loads the relevant snapshot/status on entry, and stores form values and scroll positions on the window across tab switches. Viewer sees read-only Organization/Activity and saves appearance only to device preferences. Windows Host manual navigation and Discover draft retention passed with a disposable empty library at 1200x800; Viewer, populated-library, Linux, and negotiated role-specific visual results remain unavailable.
- [ ] **U-03 Settings dialog** — General, Media & Storage, Appearance, Playback & GOON, Automation, Local Admin; vertical navigation, independent scroll and fixed Save/Cancel. H full; V device preferences. Status: partial. An in-window panel has six destinations, independent scroll positions, a fixed footer, and drafts that reset on open so Cancel discards edits. Host can edit tray, concurrency, download file/source and free-space limits, thumbnail cache ceiling, theme/layout, clip length, slideshow timing/loop/shuffle, metronome, and export reminders with input validation before queueing a save. The panel stays open while the Host service applies the save and shows any failure next to the draft. Viewer sees device appearance controls only. Windows Host manual with disposable libraries confirmed General, Media & Storage, Appearance, and Playback & GOON layouts, invalid input staying open, Cancel restoring the old concurrency value, a valid concurrency save, and metronome persisting after save/reopen. The remaining Server settings fields, Local Admin actions, Viewer and Linux manual are open.
- [ ] **U-04 Theme/accessibility** — supported palettes as Slint tokens that update every component. Status: partial. The fifteen named browser palettes, including all eight GTK presets, now update window, library cards, selection and settings surfaces; legacy browser names map to their current palettes. Slint standard controls switch light/dark mode with the saved theme; `system` and `gtk-system` use the operating-system palette. Body, card, muted and selection text contrast for the fifteen palettes has automated checks. Windows Host manual passed switching from system to Linen and then Midnight at 1200×800, including the settings dialog. GTK palette visuals, custom accents on standard controls, focus, font/window/display scaling, Viewer and Linux visual results remain open.

## Library and editing

- [ ] **L-01 Browse/hierarchy** — non-overlapping grid and table/list, bounded thumbnail cache, stable headers, source/group navigation and reassignment. H/V read-only as negotiated. Status: partial. The native Library renders separate adaptive grid and table layouts using the saved layout value; the table header remains outside its scroller. Windows Host manual verified both layouts, the nested source tree and a filtered table row using 120 disposable sample images. Thumbnail cards/cache, source reassignment, Viewer and Linux visual results remain open.
- [ ] **L-02 Query/continuity** — search, sort, media-type, tag, rating, review, size, creator, source and group filters; previous/next; preserve query, scroll and selection. Status: partial. Native Library exposes the listed filters (including all/any/excluded tags and unknown size), keeps their controls across workspace switches, retains selection, and remembers scroll per cursor. After a page renders, a guarded next-turn restoration applies its remembered scroll position so the previous page geometry cannot clamp it; stale request IDs cannot restore an old position. New filter searches and source/group navigation clear stale cursors and scroll; older page results cannot replace the current query. The inspector uses retained selection even when the selected item is on another page; an automated test covers this case. Windows Host manual with 120 disposable items verified 100/20 pagination, previous/next, a retained off-page selection and inspector, filename search, source navigation with the search text retained, and grid/table switching. The new scroll restoration timing, all filter combinations, Viewer and Linux manual results remain open.
- [ ] **L-03 Inspect/review** — metadata, provenance, rating, approve, undo and review actions. H/V per permission. Status: partial. Host rating, approval and guarded undo now call a typed media service directly; Server's existing rating, approval, undo and bulk rating endpoints adapt the same service while retaining response fields. The native inspector offers Undo for one selected reviewed item and refreshes its review token when a page updates. Service/HTTP review tests, stale undo and Viewer/shutdown/maintenance denial tests use disposable libraries. Detailed provenance and review presentation, Viewer role negotiation, Windows/Linux manual results, and other media mutations remain open.
- [ ] **L-04 Tags/organization** — media tag add/remove, inherited group tags, groups, source assignment, tag administration and source-tag review/rules. H full; V restricted. Status: partial. Native Host bulk tag add now calls the typed media service shared with Server's existing bulk endpoint; the service normalizes tags, deduplicates IDs, reports missing items, and denies Viewer/shutdown/maintenance access. Direct/HTTP parity and Viewer denial are automated. Tag removal, group and source editing, rule management, Viewer negotiated edits, and Windows/Linux manual results remain open.
- [ ] **L-05 Bulk/file actions** — accurate selection count and limits; refresh, local import, open/reveal, deletion confirmation and non-destructive state refresh. H full; V cannot open/reveal/import/local-delete. Status: partial. Viewer now hides file deletion and local metadata refresh, and its client denies both commands before any network request (automated denial test). Open/reveal, complete bulk UX, server-side role authority, and Windows/Linux manual results remain open.
- [ ] **L-06 Queue actions** — replace/append Player queue and atomically retain it. H/V playback-capable only. Status: partial. Replace, append, and remove now queue a complete native preference snapshot to a bounded dedicated writer; atomic file replacement and newest-snapshot coalescing have automated tests. Queue reorder, advance, shuffle, repeat, playback-capability negotiation, and Windows/Linux manual results remain open.

## Player, sessions and modes

- [ ] **P-01 Renderer** — bundle libmpv, Slint OpenGL texture plus software fallback, efficient stills and safe teardown. H/V permitted stream only. Status: absent.
- [ ] **P-02 Controls** — video/audio/animated/still; pause, seek, duration, volume, speed, loop, fullscreen, loading/errors and workspace persistence. Status: partial (still preview/fullscreen only).
- [ ] **P-03 Queue/clips** — reorder/remove/advance/shuffle/repeat, slideshow and boundaries; clip edit/progress/cancel/error/result. Status: partial.
- [ ] **P-04 Remote streaming** — validated range seeking, reject redirects/nested playlists/unvalidated mpv URLs. Status: partial. Server now has an additive ID-based `/api/media/:id/stream` endpoint with single-range validation, 200/206/416 and HEAD behavior; Viewer still-image loads use its pinned Tailnet origin and ID. Parser and HTTP byte/header tests use a disposable library. Native libmpv use, playlist/redirect validation in the renderer, clip boundaries, role negotiation, and Windows/Linux manual results remain unavailable.
- [ ] **P-05 Session authority** — consume deterministic Rust session effects and persist monotonic session state. Status: partial; `session.rs`/`native.rs` tests exist, and Host calls `services::session` directly while Server retains the same session routes and payloads through adapters. Direct/HTTP parity and shutdown/maintenance admission are tested. The native Player now renders the authoritative phase, active time, tempo, event and instruction through a bounded independent snapshot lane with cancellable shutdown. Native effect consumption, negotiated Viewer permissions, and Windows/Linux manual results remain unavailable.
- [ ] **P-06 Modes/cues** — feed, portrait wall, review, slideshow, GOON, Cock Hero; native metronome/audio/speech, Linux speech engine, beats, soundtrack/connectors, effects, stale-cue prevention and multi-video decoder/focus policy. Status: absent.

## Manage

- [ ] **M-01 Discover** — provider capability/status/auth, query/result-type/sort/page/cancel/errors, selection/select-all/add/download. H/V permitted remote subset. Status: partial.
- [ ] **M-02 Organization/sources** — nested group CRUD, source edit/inspect/resync/pause/resume/remove/log and clear remove-vs-files distinction. H full; V restricted. Status: partial.
- [ ] **M-03 Activity** — active/queued/retrying totals, global/per-source control, current file/retry/errors and hidden/tray updates. Status: partial. Native Host reads typed `services::downloads::status` directly and exposes per-source pause/resume controls backed by the shared service; native Viewer denies global and per-source Host-only controls before network access. Server adapters retain all download paths and payloads. Direct/native/HTTP status parity and global/per-source control parity, shutdown denial, maintenance denial, and global-pause precedence are tested with disposable libraries. Activity snapshots now refresh through an independent coalescing lane even while the window is hidden or other work is busy; current file, retry and errors are rendered. Windows Host manual passed empty-library totals and global pause/resume at 1200×800. Populated activity, Viewer, Linux, and Server-side role results remain unavailable.
- [ ] **M-04 Import/export** — source-list, metadata and package workflows with native dialogs, validation/conflicts/progress/cancel. H full; V explicit remote exports only. Status: partial. Host Manage → Organization can save a source-list JSON file through a native save dialog and atomic file replacement, and import a validated JSON source list through a native file picker. The typed export/import operations share Server `GET /api/export` and `POST /api/import` payloads; source creation is a shared service that reports duplicates. Viewer direct import/export is denied. Direct/HTTP source and duplicate parity, parser rejection, Viewer, shutdown and maintenance admission have automated tests. Windows and Linux manual results are unavailable for these new native entry points. Metadata/package workflows, detailed conflict choices, progress and cancellation remain open.

## Settings, admin and lifecycle

- [ ] **S-01 General** — concurrency, startup/tray, remote config, listening addresses/failure. H full; V preferences. Status: partial.
- [ ] **S-02 Media/Storage** — tool/download/media settings, summaries, cache and cleanups. H full; V read-only. Status: backend only.
- [ ] **S-03 Appearance/Playback/Automation** — all durable reference controls, previews and save/cancel semantics; opt-in classification, backup reminders and export. H/V appropriate fields. Status: partial. Viewer theme and library layout persist in native device preferences without patching Host settings; older preferences migrate with defaults. All fifteen named browser palettes now apply to native surfaces and light/dark controls. Remaining settings controls and preview/save/cancel semantics remain open.
- [ ] **S-04 Local Admin** — status/maintenance cards, selectable backup details, jobs, setup rerun, confirmation and restart signal. H/S only. Status: partial. Host recovery status now returns typed backup and job records; the in-window Settings → Local Admin tab offers a backup selector with size and creation time, preserves selection by backup ID across refreshes, and uses the selected ID for validation or restore. It also exposes the backend maintenance actions with their required confirmation phrases. Viewer recovery remains denied before network access. Windows Host manual with a disposable 120-item library confirmed the tab, backup creation, selector details, and validation result. Setup rerun, restart handling, complete action-specific cards, Viewer and Linux checks remain open.
- [ ] **S-05 Maintenance** — override clear; rating/evidence reset/requeue; group flatten/delete; tag/rule clear; session/CH history; thumbnail/cache rebuild; reconcile/backfill; staged reset; P-HAR removal; archive cleanup. Require backup, quiescence, exclusion, progress and recovery. Status: partial. Native Local Admin now lists the eleven existing backend maintenance kinds, shows the required phrase, blocks its Run control until it matches, and displays job phase/message/error from the shared jobs service. Backend safety backups and quiescence remain in the maintenance controller. Action-specific descriptions, rule clearing, reset/restart recovery, cancellation, and Windows/Linux manual destructive-action checks remain open.
- [ ] **S-06 Classifier** — opt-in install/evaluate/configure/repair/cancel/self-test/status with platform/dependency/model/checksum/rights validation, honest blocked reasons and human/automatic provenance. H/S only. Status: backend partial/native absent.
- [ ] **D-01 Host lifecycle** — first run/reopen, dialogs, safe data-dir move, single instance, tray/startup/shortcuts/window state and graceful quit. Status: partial.
- [ ] **D-02 Viewer lifecycle** — saved-host list/edit/remove, unknown-field-preserving migration, stable identity, negotiation, validated retries/reconnect/switching. Status: partial. Viewer native device preferences preserve unknown JSON fields through appearance/layout saves and default missing legacy fields. The native Viewer has a Switch Host action that returns to its saved-host picker, where the next connection is revalidated against Tailscale and instance identity. The Server handshake advertises a read-only Viewer permission set that the native client applies to commands and visible controls. After a network failure, Viewer retries read requests against its pinned Tailnet address only after the Host handshake reconfirms the same instance, protocol and permissions; writes are not retried. Automated tests cover identity replacement, permission change and missing identity rejection. Re-resolving a moved Host, user-visible reconnect state, Server-side authenticated role authority, and Windows/Linux Viewer manual results remain open.

## Compatibility and verification

- [ ] **C-01 Diagnostics** — rotating redacted logs, opt-in verbosity and visible locations. Status: implemented; Linux manual verification pending. Host and Server use daily rotation with eight retained files, redact secret-shaped values before persistent output, default to `info` with explicit `RUST_LOG` opt-in, and show the log location in Host Manage or Server startup output. The shared diagnostics service bounds and redacts native/HTTP log reads, including source errors; Viewer native log access is denied. Windows Host manual passed using a disposable empty library: Manage Settings showed the daily log path and readable entries, and View diagnostic log loaded them. Windows Server startup printed the disposable log directory and then correctly reported an occupied default port; a full Server listening smoke is still unavailable. Linux manual remains unavailable.
- [ ] **C-02 Bundles** — libmpv for both editions; ffmpeg/ffprobe/isolated gallery-dl for H; install-relative resolution/overrides, pinned checksums/notices and packaging failure on missing tools. Status: absent.
- [ ] **C-03 Installers** — collision-free Windows NSIS/MSI and Linux Debian/AppImage with identity/scope/upgrade/uninstall-data guarantees. Status: partial (binaries only).
- [ ] **V-01 Evidence ledger** — each record requires direct-service/HTTP tests, H/V permission test and Windows/Linux manual result with disposable libraries. Existing tests cover backend/session/pagination/browser/package contracts; a Windows Host smoke now covers empty-library navigation, filter controls, inspector scrolling, Manage destination retention, and settings dialog reachability. Populated-library, Viewer, Linux, and installer manual evidence remains unavailable.
- [ ] **V-02 Final gates** — fmt, locked check/test, strict Clippy, Server browser tests, native UI/package smokes, fresh/upgrade installers, offline/occupied-port/tray tests and browser-engine package/process audit.

## Feature evidence in progress

- **Settings, storage and discovery reads (A-01/A-04/M-01):** Native entry point Host Manage → Settings and Discover; service operations `services::settings::read`, `services::storage::dashboard` and `services::discovery::providers`; Server adapters retain `GET /api/settings`, `GET /api/storage` and `GET /api/search/providers` payloads. Host/Server use local data; remote settings hide executable paths, while Viewer local storage and provider registry calls are forbidden. Automated tests cover direct/HTTP parity for all three reads, local Host versus Server integration availability, Viewer path denial, storage Viewer and shutdown denial, and maintenance exclusion with disposable libraries. Windows manual: the Host Manage Settings and Discover screens opened in a disposable empty library in an earlier smoke; the new service operations were not independently visually tested. Linux manual: unavailable. Settings writes, discovery search and remaining services keep A-01 open.
- **Viewer role and switching (A-04/D-02):** Native entry points Viewer handshake, Library, Player, Manage and Switch Host; `RemoteClient` enforces the advertised read-only `ViewerPermissions` before network requests, while `GET /api/system/info` adds the permission set without changing existing fields. Viewer may read and play permitted media but may not edit the library, control sessions or manage Host downloads under the default role. After a failed read request, automatic retry rechecks the pinned Host identity, protocol and permissions; changed identity or permissions require Switch Host. Automated tests cover a legacy handshake default, read/edit/session/discovery denial without network access, system response, reconnect identity/permission rejection, and missing identity rejection; locked workspace compilation covers the Switch Host path. Windows and Linux Viewer manual: unavailable without a connected Tailnet Host. Server-side authenticated role authority, moved-host discovery, reconnect UI, and a live retry smoke remain open.
- **Library list read:** Native entry point `LocalClient::library`; service operation `services::library::list`; Server adapter `GET /api/media`. Host and Server may read their local library; a Viewer local library call is denied, and the native remote client checks its advertised read permission before network access. Automated tests cover direct/native/HTTP query and cursor parity, invalid-query payloads, Viewer local and negotiated read denial, shutdown denial, and maintenance denial with disposable libraries. Windows manual: Host launched from the isolated `native_manual` harness with 120 disposable images; the Library, filters, 100/20 pagination and nested source counts rendered at 1200×800. Linux manual: unavailable. Server-side authenticated Viewer role authority, A-01, A-04, L-02 and V-01 remain open.
- **Library filters:** Native entry point Library filter controls and source/group tree; service operation `services::library::list`; Server adapter `GET /api/media`. Host uses direct service reads; Viewer remote reads require the advertised library-read permission before network access. Automated tests cover creator and size parity, byte-input validation, inherited tag filtering, navigation cursor reset, pagination and off-page inspector retention. Windows manual: an empty disposable library showed every filter control; a second disposable library with 120 generated samples showed 100/20 pages, previous/next, a second-page selection retained in the inspector after returning to page one, search for `sample-060`, source navigation retaining the search value, and grid/table layouts at 1200×800. The sample libraries were removed after the runs. Linux manual: unavailable. Scroll restoration and full filter combinations remain open, so L-02 remains partial.
- **Manage destinations and settings panel:** Native entry point Manage destination tabs and the in-window Settings panel; service operations include typed download status and current route-adapted discovery and settings snapshots. Host can open all four Manage destinations and six settings destinations; Viewer Organization/Activity mutations are hidden and appearance stays on its device. The panel retains independent destination scroll positions, initializes editable drafts from the current snapshot, and keeps invalid drafts open with actionable input errors. Automated result: Slint compilation, workspace tests, input parser tests, native preference migration and remote settings denial. Windows manual: Host opened Discover, Organization, Activity, Settings and the in-window panel with 120 disposable items at 1200×800; General, Media & Storage, Appearance, Playback & GOON, and Automation rendered. An invalid concurrency value stayed in the panel with an error, Cancel restored the original 6, and saving 7 closed the panel after the service succeeded; reopening showed 7. A later disposable Host smoke showed the new source/cache limits and clip/metronome controls, and a metronome change persisted after save/reopen. The corrected layout packed controls at the top with fixed Save/Cancel. Linux and Viewer manual: unavailable. U-02 and U-03 remain open because remaining settings controls and Local Admin actions are incomplete.
- **Diagnostics (C-01):** Native entry point Host Manage → Settings → View diagnostic log, with the current path in diagnostics; service operations `services::diagnostics::{rotating_file_writer, read_tail, location, redact}`; Server adapters retain `GET /api/log` plain text and `GET /api/sources/:id/log` JSON shape. Host may view its local log; Viewer native access is denied before network and its settings panel hides the log action. Automated tests cover pre-write redaction, daily filename discovery, bounded tail selection, direct native/HTTP parity, source-log redaction, and Viewer denial with disposable data. Windows manual: Host opened Manage Settings at 1200×800 with a disposable empty library, observed the current daily log path and startup entries, and used View diagnostic log to load the entries. Server CLI startup with a separate disposable library printed the log directory, then reported the already occupied port 42168; a listening Server smoke could not run on that port. Linux manual: unavailable.
- **Session authority (P-05):** Native entry point Host Player session controls and status; service operation `services::session::{current,start,control}`; Server adapters retain `GET /api/session`, `POST /api/session/start`, and `POST /api/session/command`. Host uses the direct service and Viewer uses remote snapshots and commands pending role negotiation. Automated tests cover service/HTTP state parity, maintenance/shutdown denial, runner completion after a rejected concurrent Start, Ready-state replacement denial, terminal summary idempotency, and native rendering of phase and active time. Windows manual: Host started, paused, resumed, and completed a session in a disposable empty library; the Player showed the authoritative status, phase, BPM and advancing active time. Viewer and Linux manual: unavailable. Effect consumption and role negotiation remain open.
- **Activity (M-03):** Native entry point Host Manage → Activity; service operations `services::downloads::{status,pause,resume,pause_source,resume_source}`; Server adapters retain `/api/downloads/status`, `/api/downloads/pause`, `/api/downloads/resume`, and source control paths. Host may control downloads; native Viewer denies Host-only controls before network access. Automated tests cover direct/native/HTTP status and control parity, shutdown and maintenance exclusion, global-pause precedence, and Viewer denial with disposable libraries. Windows manual: Host opened Activity at 1200×800 in an empty disposable library, saw zero totals, paused downloads and saw the paused state, then resumed and saw active/queued/retrying totals. Populated activity and Viewer manual: unavailable. Linux manual: unavailable. Server-side Viewer role authority remains open.
- **Backup selection (S-04):** Native entry point Host Manage → Settings → Local Admin in the existing window; service operations `services::backup::{list,snapshot,download_path}` return typed backup and job records and validate download IDs. `services::jobs::{list,get,start}` owns maintenance admission; Host recovery and Server admin adapters use it for create, validate, restore and other job starts. Server retains `GET /api/admin/backups` and the existing job paths and payloads. Viewer and shutdown inventory, download and job requests are denied at the service boundary; Viewer native recovery is denied before network access. Automated tests cover direct/HTTP inventory and job-list parity, confirmation rejection parity, download traversal rejection, Host record enumeration, Viewer and shutdown denial, and selection continuity by ID. Windows manual: Host created a backup from a disposable 120-item library in the Local Admin tab; the selector showed its ID, byte size and creation time, and Validate completed with “Backup is valid.” The disposable library was removed after close. Linux manual: unavailable. Restore, full maintenance cards and restart handling are open.
- **Source-list import/export (A-01/M-04):** Native entry points Host Manage → Organization → Export source list and Import source list. `services::export::{source_list,parse_source_file,import_source_list}` returns typed export records, validates native JSON, restores source groups and advances the export reminder clock. `services::sources::create` shares URL admission, duplicate detection and source creation between Host and Server. Host writes JSON atomically after a save dialog or reads at most 8 MiB from an open dialog; Server retains `GET /api/export`, `POST /api/import`, and source-add payloads. Viewer native import/export is denied before network access. Automated tests cover direct/HTTP source and duplicate parity, reminder persistence, invalid native file rejection, Viewer/shutdown/maintenance denial, and native Viewer denial. Windows manual: unavailable for these entry points after the user requested desktop control be relinquished. Linux manual: unavailable. Metadata/package workflows, cancellation and detailed conflict choices remain open.
- **Theme and accessibility (U-04):** Native entry point Host Manage → Settings → Save native theme and Viewer device appearance; shared Host settings still use the Server settings adapter while Viewer persists only local native preferences. Host may change its saved theme; Viewer may change only its device theme. Automated tests cover fifteen palette body/card/muted/selection contrast, legacy browser aliases and Viewer preference migration; Slint compilation checks the light/dark control binding. Windows manual: Host switched a disposable empty library from system to Linen and Midnight at 1200×800; the window, controls and settings dialog visibly switched. GTK palette visual checks, Viewer and Linux manual: unavailable. Custom standard-control accents, focus and scaling remain open.

## API, commands and history

The reference API covers system/OOBE; media, thumbnails, tags and source-tag rules; sources/groups/downloads; search; settings/remote/storage; import/export/chpack; stats/log; admin jobs/backups/P-HAR; session/GOON/beat maps; and Cock Hero playlist/session routes. Exact methods and paths remain in `src/routes/mod.rs`; migrations must retain them. Server CLI commands are `import-host` and `phar-intent`; former desktop bridge commands were path choosing, local import, media action, library summary and generic API request. Relevant history: `b6e98d71`, `e49cdfdf`, `e5301006`, `d4d1ca43`, `ff4ae654`, `3fc9aaca`, `45de18ef`, `1602d216`, `f74b14d9`.
